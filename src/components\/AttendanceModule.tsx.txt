import React, { memo, useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { AlertTriangle, CheckCircle2, Clock3, Database, Fingerprint, LogIn, LogOut, Plus, RefreshCw, Upload } from 'lucide-react';
import { TeamMember } from '../types';
import { desktopDatabase } from '../utils/desktopDatabase';

type AttendanceRow={id:string;employee_id:string;employee_name:string;event_type:string;event_time:string;source:string;device_id?:string;notes?:string};
type DeviceRow={id:string;name:string;device_type:string;connection_type:string;host?:string;port?:number;serial_number?:string;branch?:string;enabled:number;last_sync_at?:string};
type PayrollEmployee={id:string;name:string;job_title?:string;attendance_employee_id?:string;active:number};
type Person={id:string;name:string;subtitle?:string};
const uid=(p:string)=>`${p}_${Date.now()}_${Math.random().toString(36).slice(2,8)}`;
function Field({label,hint,children}:{label:string;hint?:string;children:React.ReactNode}){return <label className="block space-y-1"><span className="block text-xs font-black text-slate-700">{label}</span>{hint&&<span className="block text-[10px] leading-4 text-slate-500">{hint}</span>}{children}</label>}
function SignaturePad({onChange,ar}:{onChange:(value:string)=>void;ar:boolean}){
  const ref=useRef<HTMLCanvasElement>(null); const drawing=useRef(false);
  useEffect(()=>{const c=ref.current;if(!c)return;const ctx=c.getContext('2d');if(ctx){ctx.lineWidth=2;ctx.lineCap='round';ctx.strokeStyle='#111827';}},[]);
  const point=(e:React.PointerEvent<HTMLCanvasElement>)=>{const r=e.currentTarget.getBoundingClientRect();return {x:(e.clientX-r.left)*(e.currentTarget.width/r.width),y:(e.clientY-r.top)*(e.currentTarget.height/r.height)}};
  const down=(e:React.PointerEvent<HTMLCanvasElement>)=>{drawing.current=true;e.currentTarget.setPointerCapture(e.pointerId);const p=point(e),ctx=e.currentTarget.getContext('2d');ctx?.beginPath();ctx?.moveTo(p.x,p.y)};
  const move=(e:React.PointerEvent<HTMLCanvasElement>)=>{if(!drawing.current)return;const p=point(e),ctx=e.currentTarget.getContext('2d');ctx?.lineTo(p.x,p.y);ctx?.stroke()};
  const up=()=>{drawing.current=false;const c=ref.current;if(c)onChange(c.toDataURL('image/png'))};
  const clear=()=>{const c=ref.current;if(!c)return;c.getContext('2d')?.clearRect(0,0,c.width,c.height);onChange('')};
  return <Field label={ar?'توقيع الموظف (اختياري)':'Employee signature (optional)'} hint={ar?'يمكن التسجيل دون توقيع، أو التوقيع داخل المساحة البيضاء قبل الحضور أو الانصراف.':'Attendance can be saved with or without a signature.'}><div className="space-y-2"><canvas ref={ref} width={520} height={150} onPointerDown={down} onPointerMove={move} onPointerUp={up} onPointerCancel={up} className="w-full h-32 border-2 border-dashed rounded-xl touch-none bg-white"/><button onClick={clear} type="button" className="text-xs font-bold text-rose-600">{ar?'مسح التوقيع':'Clear signature'}</button></div></Field>
}
export const AttendanceModule=memo(function AttendanceModule({teamMembers,language='ar'}:{teamMembers:TeamMember[];language?:string}){
  const ar=language==='ar';
  const [records,setRecords]=useState<AttendanceRow[]>([]),[devices,setDevices]=useState<DeviceRow[]>([]),[summary,setSummary]=useState<any[]>([]),[payrollEmployees,setPayrollEmployees]=useState<PayrollEmployee[]>([]);
  const [employeeId,setEmployeeId]=useState(''),[signature,setSignature]=useState(''),[notes,setNotes]=useState(''),[showDevice,setShowDevice]=useState(false);
  const [device,setDevice]=useState<any>({name:'',deviceType:'GENERIC',connectionType:'CSV',host:'',port:'4370',serialNumber:'',branch:'',enabled:true});
  const [deviceAction,setDeviceAction]=useState<Record<string,string>>({});
  const [dateFrom,setDateFrom]=useState(new Date().toISOString().slice(0,10)),[dateTo,setDateTo]=useState(new Date().toISOString().slice(0,10));
  const [status,setStatus]=useState<{type:'ok'|'error'|'loading';message:string}>({type:'loading',message:ar?'جارٍ الاتصال بقاعدة البيانات…':'Connecting to database…'});
  const people=useMemo<Person[]>(()=>{const map=new Map<string,Person>();for(const e of payrollEmployees.filter(x=>x.active)){const id=e.attendance_employee_id||e.id;map.set(id,{id,name:e.name,subtitle:e.job_title})}for(const x of teamMembers.filter(x=>x.active)){if(!map.has(x.id))map.set(x.id,{id:x.id,name:x.name,subtitle:ar?'مستخدم في فريق العمل':'System team user'})}return [...map.values()]},[payrollEmployees,teamMembers,ar]);
  const employee=useMemo(()=>people.find(x=>x.id===employeeId),[people,employeeId]);
  const reload=useCallback(async()=>{if(!desktopDatabase.available()){setStatus({type:'error',message:ar?'قاعدة SQLite غير متاحة. افتح تطبيق سطح المكتب.':'SQLite unavailable. Open the desktop app.'});return}try{setStatus({type:'loading',message:ar?'جارٍ تحميل الحضور والموظفين…':'Loading attendance and employees…'});const [r,d,s,e]=await Promise.all([desktopDatabase.list<AttendanceRow>({table:'attendance_records',page:1,pageSize:200,orderBy:'event_time',direction:'DESC'}),desktopDatabase.list<DeviceRow>({table:'attendance_devices',page:1,pageSize:100,orderBy:'created_at',direction:'DESC'}),desktopDatabase.attendanceSummary({dateFrom,dateTo}),desktopDatabase.list<PayrollEmployee>({table:'payroll_employees',page:1,pageSize:500,orderBy:'name'})]);setRecords(r.rows);setDevices(d.rows);setSummary(s as any[]);setPayrollEmployees(e.rows);setStatus({type:'ok',message:ar?`تم الاتصال — ${e.rows.length} موظف و${r.rows.length} حركة حضور`:`Connected — ${e.rows.length} employees, ${r.rows.length} attendance events`})}catch(error){setStatus({type:'error',message:`${ar?'فشل تحميل بيانات الحضور':'Attendance load failed'}: ${error instanceof Error?error.message:String(error)}`})}},[dateFrom,dateTo,ar]);
  useEffect(()=>{reload()},[reload]);
  useEffect(()=>{if(!employeeId&&people[0])setEmployeeId(people[0].id)},[people,employeeId]);
  const record=async(eventType:string)=>{if(!employee)return alert(ar?'اختر موظفًا أولًا':'Select an employee first');try{await desktopDatabase.recordAttendance({id:uid('att'),employeeId:employee.id,employeeName:employee.name,eventType,eventTime:new Date().toISOString(),source:signature?'SIGNATURE':'MANUAL',signatureData:signature||undefined,notes:notes.trim()||undefined});setSignature('');setNotes('');await reload();alert(ar?(eventType==='CHECK_IN'?'تم تسجيل الحضور':'تم تسجيل الانصراف'):(eventType==='CHECK_IN'?'Check-in saved':'Check-out saved'))}catch(error){alert(`${ar?'تعذر تسجيل الحركة':'Could not save attendance'}: ${error instanceof Error?error.message:String(error)}`)}};
  const saveDevice=async()=>{if(!device.name.trim())return alert(ar?'اكتب اسم الجهاز':'Enter device name');try{await desktopDatabase.saveAttendanceDevice({...device,id:uid('device'),port:Number(device.port)||undefined});setShowDevice(false);setDevice({name:'',deviceType:'GENERIC',connectionType:'CSV',host:'',port:'4370',serialNumber:'',branch:'',enabled:true});await reload()}catch(error){alert(`${ar?'تعذر حفظ الجهاز':'Could not save device'}: ${error instanceof Error?error.message:String(error)}`)}};
  const testDevice=async(item:DeviceRow)=>{try{setDeviceAction(x=>({...x,[item.id]:ar?'جارٍ اختبار الاتصال…':'Testing connection…'}));const result=await desktopDatabase.testBiometricDevice(item);setDeviceAction(x=>({...x,[item.id]:result.message||(ar?'نجح اختبار الاتصال':'Connection test succeeded')}))}catch(error){setDeviceAction(x=>({...x,[item.id]:`${ar?'فشل الاختبار':'Test failed'}: ${error instanceof Error?error.message:String(error)}`}))}};
  const syncDevice=async(item:DeviceRow)=>{try{setDeviceAction(x=>({...x,[item.id]:ar?'جارٍ سحب سجلات الحضور…':'Downloading attendance…'}));const result=await desktopDatabase.syncBiometricDevice(item);setDeviceAction(x=>({...x,[item.id]:ar?`تم استلام ${result.received||0} واستيراد ${result.imported||0} سجل`:`Received ${result.received||0}, imported ${result.imported||0}`}));await reload()}catch(error){setDeviceAction(x=>({...x,[item.id]:`${ar?'تعذر السحب':'Sync failed'}: ${error instanceof Error?error.message:String(error)}`}))}};
  const importCsv=async(file:File)=>{try{const text=await file.text();const lines=text.split(/\r?\n/).filter(Boolean);const head=lines.shift()?.split(',').map(x=>x.trim().toLowerCase())||[];const rows=lines.map(line=>{const values=line.split(',').map(x=>x.trim());const obj=Object.fromEntries(head.map((h,i)=>[h,values[i]]));const person=people.find(x=>x.id===obj.employee_id||x.name===obj.employee_name);return {id:uid('att'),employeeId:person?.id||obj.employee_id,employeeName:person?.name||obj.employee_name||obj.employee_id,eventType:(obj.event_type||'CHECK_IN').toUpperCase(),eventTime:obj.event_time||new Date().toISOString(),source:'CSV',deviceId:obj.device_id||undefined,notes:obj.notes||undefined}}).filter(x=>x.employeeId);const result=await desktopDatabase.importAttendance(rows);alert(`${ar?'تم استيراد':'Imported'} ${(result as any).imported}`);await reload()}catch(error){alert(`${ar?'تعذر استيراد الملف':'Import failed'}: ${error instanceof Error?error.message:String(error)}`)}};
  const inputClass='w-full border border-slate-300 rounded-xl px-3 py-2.5 bg-white text-slate-900 focus:outline-none focus:ring-2 focus:ring-indigo-500';
  return <div className="space-y-5" dir={ar?'rtl':'ltr'}>
    <div className={`rounded-xl border px-4 py-3 text-xs font-bold flex items-center gap-2 ${status.type==='error'?'border-rose-300 bg-rose-50 text-rose-700':status.type==='ok'?'border-emerald-300 bg-emerald-50 text-emerald-700':'border-amber-300 bg-amber-50 text-amber-700'}`}>{status.type==='error'?<AlertTriangle size={17}/>:status.type==='ok'?<CheckCircle2 size={17}/>:<RefreshCw size={17} className="animate-spin"/>}{status.message}</div>
    <div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-black text-xl flex items-center gap-2"><Fingerprint className="text-indigo-600"/>{ar?'الحضور والانصراف والبصمة والتوقيع':'Attendance, biometric & signature'}</h2><p className="text-xs text-slate-500">{ar?'يقرأ الموظفين المستقلين من قاعدة المرتبات، ولا يقتصر على مستخدمي البرنامج.':'Uses independent employees as well as system users.'}</p></div><div className="flex gap-2"><label className="px-3 py-2 border rounded-xl text-xs font-bold cursor-pointer flex gap-1"><Upload size={16}/>{ar?'استيراد CSV':'Import CSV'}<input type="file" accept=".csv" className="hidden" onChange={e=>e.target.files?.[0]&&importCsv(e.target.files[0])}/></label><button onClick={()=>setShowDevice(true)} className="px-3 py-2 bg-slate-900 text-white rounded-xl text-xs font-bold flex gap-1"><Plus size={16}/>{ar?'إضافة جهاز بصمة':'Add device'}</button></div></div>
    <div className="grid lg:grid-cols-3 gap-4"><div className="lg:col-span-2 border rounded-2xl p-4 space-y-4"><Field label={ar?'الموظف الذي ستسجل له الحركة *':'Employee *'} hint={ar?'تظهر هنا سجلات تبويب الموظفين، بالإضافة إلى مستخدمي فريق العمل غير المرتبطين بموظف.':'Includes employee records and unmatched system users.'}><select value={employeeId} onChange={e=>setEmployeeId(e.target.value)} className={inputClass}><option value="">{ar?'اختر الموظف':'Select employee'}</option>{people.map(x=><option key={x.id} value={x.id}>{x.name}{x.subtitle?` — ${x.subtitle}`:''}</option>)}</select></Field><SignaturePad onChange={setSignature} ar={ar}/><Field label={ar?'ملاحظة الحركة':'Attendance note'} hint={ar?'اختياري: سبب التأخير، مهمة خارجية، أو أي توضيح.':'Optional note such as lateness reason or external assignment.'}><input value={notes} onChange={e=>setNotes(e.target.value)} className={inputClass}/></Field><div className="grid grid-cols-2 gap-2"><button disabled={!employee||status.type==='error'} onClick={()=>record('CHECK_IN')} className="bg-emerald-600 disabled:bg-slate-400 text-white p-3 rounded-xl font-bold flex justify-center gap-2"><LogIn/>{ar?'تسجيل حضور الآن':'Check in now'}</button><button disabled={!employee||status.type==='error'} onClick={()=>record('CHECK_OUT')} className="bg-rose-600 disabled:bg-slate-400 text-white p-3 rounded-xl font-bold flex justify-center gap-2"><LogOut/>{ar?'تسجيل انصراف الآن':'Check out now'}</button></div></div><div className="border rounded-2xl p-4"><h3 className="font-black mb-3 flex gap-2"><Database size={18}/>{ar?'أجهزة البصمة المسجلة':'Registered biometric devices'}</h3>{devices.length?devices.map(x=><div key={x.id} className="border-b py-3 text-xs space-y-2"><div><b>{x.name}</b><div>{x.device_type} · {x.connection_type}</div><div className="text-slate-500">{x.host||x.serial_number||x.branch||'—'}{x.port?` : ${x.port}`:''}</div></div><div className="flex flex-wrap gap-2"><button type="button" onClick={()=>testDevice(x)} className="rounded-lg border border-indigo-200 bg-indigo-50 px-2.5 py-1.5 font-bold text-indigo-700">{ar?'اختبار الاتصال':'Test connection'}</button>{x.connection_type!=='CSV'&&<button type="button" onClick={()=>syncDevice(x)} className="rounded-lg bg-emerald-600 px-2.5 py-1.5 font-bold text-white">{ar?'سحب الحضور':'Sync attendance'}</button>}</div>{deviceAction[x.id]&&<div className="rounded-lg bg-slate-50 p-2 leading-5 text-slate-700">{deviceAction[x.id]}</div>}</div>):<div className="text-xs text-slate-400">{ar?'لا توجد أجهزة مضافة':'No devices'}</div>}</div></div>
    <div className="border rounded-2xl p-4 space-y-3"><h3 className="font-black">{ar?'ملخص الحضور خلال فترة':'Attendance summary by period'}</h3><div className="flex flex-wrap gap-3 items-end"><Field label={ar?'تاريخ البداية':'Start date'}><input type="date" value={dateFrom} onChange={e=>setDateFrom(e.target.value)} className={inputClass}/></Field><Field label={ar?'تاريخ النهاية':'End date'}><input type="date" value={dateTo} onChange={e=>setDateTo(e.target.value)} className={inputClass}/></Field><button onClick={reload} className="p-2.5 border rounded-lg"><Clock3 size={18}/></button></div><div className="overflow-auto"><table className="w-full text-xs"><thead><tr className="bg-slate-50"><th className="p-2 text-right">{ar?'الموظف':'Employee'}</th><th>{ar?'أول حضور':'First in'}</th><th>{ar?'آخر انصراف':'Last out'}</th><th>{ar?'ساعات العمل':'Worked hours'}</th><th>{ar?'عدد الحركات':'Events'}</th></tr></thead><tbody>{summary.map(x=><tr key={x.employee_id} className="border-t"><td className="p-2 font-bold">{x.employee_name}</td><td>{x.first_in?new Date(x.first_in).toLocaleString():'—'}</td><td>{x.last_out?new Date(x.last_out).toLocaleString():'—'}</td><td>{Number(x.worked_hours||0).toFixed(2)}</td><td>{x.events}</td></tr>)}</tbody></table></div></div>
    <div className="border rounded-2xl overflow-auto"><table className="w-full text-xs"><thead className="bg-slate-50"><tr><th className="p-3 text-right">{ar?'الموظف':'Employee'}</th><th>{ar?'الحركة':'Event'}</th><th>{ar?'الوقت':'Time'}</th><th>{ar?'المصدر':'Source'}</th></tr></thead><tbody>{records.map(x=><tr key={x.id} className="border-t"><td className="p-3 font-bold">{x.employee_name}</td><td>{x.event_type==='CHECK_IN'?(ar?'حضور':'Check in'):(ar?'انصراف':'Check out')}</td><td>{new Date(x.event_time).toLocaleString()}</td><td>{x.source}</td></tr>)}</tbody></table></div>
    {showDevice&&<div className="fixed inset-0 bg-black/50 z-[120] flex items-center justify-center p-4"><div className="bg-white rounded-2xl p-5 w-full max-w-2xl space-y-4"><h3 className="font-black">{ar?'بيانات جهاز البصمة':'Biometric device details'}</h3><div className="grid md:grid-cols-2 gap-3"><Field label={ar?'اسم الجهاز *':'Device name *'} hint={ar?'اسم تعريفي داخل النظام، مثل جهاز المدخل الرئيسي.':'A friendly name used inside the system.'}><input value={device.name} onChange={e=>setDevice({...device,name:e.target.value})} className={inputClass}/></Field><Field label={ar?'شركة أو نوع الجهاز':'Device brand / type'}><select value={device.deviceType} onChange={e=>setDevice({...device,deviceType:e.target.value})} className={inputClass}><option value="GENERIC">GENERIC</option><option value="ZKTECO">ZKTeco</option><option value="ANVIZ">Anviz</option><option value="SUPREMA">Suprema</option><option value="HIKVISION">Hikvision</option><option value="RONALD_JACK">Ronald Jack</option><option value="NITGEN">Nitgen</option></select></Field><Field label={ar?'طريقة الاتصال':'Connection method'} hint={ar?'CSV للاستيراد من ملف، DIRECT للاتصال الشبكي المباشر، وSDK عندما توفر الشركة مكتبتها الرسمية.':'CSV file import, DIRECT network driver, or vendor SDK bridge.'}><select value={device.connectionType} onChange={e=>setDevice({...device,connectionType:e.target.value})} className={inputClass}><option value="CSV">CSV</option><option value="DIRECT">DIRECT — اتصال مباشر</option><option value="SDK">SDK — موصل الشركة</option></select></Field><Field label={ar?'عنوان IP أو اسم المضيف':'IP address / host'} hint={ar?'يستخدم فقط مع أجهزة الشبكة، مثال 192.168.1.201.':'Only for network devices.'}><input value={device.host} onChange={e=>setDevice({...device,host:e.target.value})} className={inputClass}/></Field><Field label={ar?'رقم المنفذ':'Network port'} hint={ar?'الافتراضي في كثير من أجهزة ZKTeco هو 4370.':'Common ZKTeco default is 4370.'}><input inputMode="numeric" value={device.port} onChange={e=>setDevice({...device,port:e.target.value})} className={inputClass}/></Field><Field label={ar?'الرقم التسلسلي للجهاز':'Device serial number'}><input value={device.serialNumber} onChange={e=>setDevice({...device,serialNumber:e.target.value})} className={inputClass}/></Field><Field label={ar?'الفرع أو موقع الجهاز':'Branch / location'}><input value={device.branch} onChange={e=>setDevice({...device,branch:e.target.value})} className={inputClass}/></Field></div><div className="rounded-xl bg-amber-50 border border-amber-200 p-3 text-xs text-amber-800">{ar?'يدعم النظام ثلاث طبقات: CSV يعمل دائمًا، DIRECT يختبر الاتصال الشبكي ويستخدم Driver مباشر عندما يكون البروتوكول مدعومًا، وSDK يبحث عن موصل الشركة داخل مجلد biometric-sdk. لن يعتبر الجهاز متوافقًا إلا بعد نجاح اختبار الاتصال وسحب السجلات.':'Three layers are supported: CSV, direct network drivers, and vendor SDK bridges. A device is only considered operational after connection and attendance download tests succeed.'}</div><div className="flex justify-end gap-2"><button onClick={()=>setShowDevice(false)} className="px-4 py-2 border rounded-xl">{ar?'إلغاء':'Cancel'}</button><button onClick={saveDevice} className="px-4 py-2 bg-indigo-600 text-white rounded-xl">{ar?'حفظ الجهاز':'Save device'}</button></div></div></div>}
  </div>
});
